<?xml version="1.0" encoding="ASCII"?>
<routes xmlns="http://camel.apache.org/schema/spring">
    <route>
        <from uri="switchyard://PersonaServiceInterface"/>
        <log message="PersonaServiceInterface - message received: ${body}"/>
        <to uri="switchyard://PersonaSource"/>
    </route>
</routes>